rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Helper functions
    function isAuthenticated() {
      return request.auth != null;
    }

    function isAdmin() {
      return isAuthenticated() && 
        (request.auth.token.admin == true || 
         request.auth.token.role == 'admin' ||
         exists(/databases/$(database)/documents/admins/$(request.auth.uid)));
    }

    function isOwner(userId) {
      return isAuthenticated() && request.auth.uid == userId;
    }

    // Platform data storage - Direct client access is strictly blocked.
    // All platform data operations must go through the authenticated backend API.
    match /app_data/{document=**} {
      allow read, write: if false;
    }

    // Student Specific Documents
    match /students/{studentId} {
      allow read, write: if isOwner(studentId) || isAdmin();
      
      match /progress/{lessonId} {
        allow read, write: if isOwner(studentId) || isAdmin();
      }
      
      match /attempts/{attemptId} {
        allow read, write: if isOwner(studentId) || isAdmin();
      }

      match /weakness_profile/{profileId} {
        allow read, write: if isOwner(studentId) || isAdmin();
      }
    }

    // Admin Documents
    match /admins/{adminId} {
      allow read: if isAuthenticated();
      allow write: if isAdmin();
    }

    // Default Deny All other paths
    match /{document=**} {
      allow read, write: if false;
    }
  }
}

